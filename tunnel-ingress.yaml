apiVersion: extensions/v1beta1
kind: DaemonSet
metadata:
  labels:
    k8s-app: tunnel-ingress
  name: tunnel-ingress
  namespace: kube-system
spec:
  template:
    metadata:
      annotations:
        cmd.nu/tunnel-ingress: ""
      labels:
        k8s-app: tunnel-ingress
    spec:
      hostNetwork: true
      containers:
        - args:
          - sleep
          - "10000"
          image: ubuntu:yakkety
          imagePullPolicy: Always
          name: router
          securityContext:
            privileged: true
      terminationGracePeriodSeconds: 5
